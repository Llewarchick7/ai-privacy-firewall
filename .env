SECRET_KEY = your_super_secret_key
# Token required in X-Device-Token header for /api/dns/dns-queries/batch
DEVICE_INGEST_TOKEN = change_me_device_ingest_token
# Using SQLite for easier testing - change to PostgreSQL for production
DATABASE_URL = sqlite:///./ai_firewall.db

# OAuth Credentials for Google
GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_CLIENT_SECRET=your_client_secret_here

# Backend OAuth callback used by the frontend
OAUTH_REDIRECT_URI=http://localhost:8000/api/users/oauth/google/callback

# Frontend URL for redirects
FRONTEND_URL=http://localhost:3000

# Frontend API Base URL (not required by backend; informational)
FRONTEND_API_BASE=http://localhost:3000/api

